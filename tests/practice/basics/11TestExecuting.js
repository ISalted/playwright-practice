/*
    - npx playwright test
    A command associated with Playwright used to execute automated tests for web applications.Playwright is a browser automation library that allows you to create and run tests in Chrome, Firefox, and WebKit browsers.

    - npx playwright test--project = chromium--headed
    The command runs tests only in Chromium, displaying them on the screen.

    - npx playwright test loginTests.spec.js--project = chromium--headed
    The command runs only the specific test file.

    - npx playwright test - g "LogIn test" --project = chromium--headed
    The command runs only a specific test, in our case, "LogIn test." The - g flag specifies a particular test.

    - npx playwright show - report
    Displays the report

*/
